import styled from 'styled-components';
import { Button } from '../Button';

export const StyledLoadPostsButton = styled(Button)`
  margin: 0 auto;
  display: block;
  min-width: 300px;
`;

export const LoadingSpinnerContainer = styled.div`
  justify-content: center;
  display: flex;
`;
